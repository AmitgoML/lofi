"""Image generator prompt constants — versioned."""

# v1: default Titan negative prompt (keeps out common artifacts and unintended text)
DEFAULT_NEGATIVE_PROMPT_V1 = (
    "blurry, low quality, distorted, watermark, text overlay, logo, signature, "
    "overexposed, underexposed, pixelated, noisy, amateur, low resolution, "
    "disfigured, ugly, out of frame, duplicate, morbid, mutilated"
)
