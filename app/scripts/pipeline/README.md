# Pipeline scripts

Operational scripts for backfill, reconciliation, and manual connector runs.

Planned:

- `run_daily_ingestion.py` — invoke all platform connectors (P4-07)
- `run_backfill.py` — bounded historical load per org/platform (P4-08)
- `reconcile.py` — post-ingestion validation harness

Not implemented in scaffolding phase.
