[project]
name = "lofi"
version = "0.1.0"
description = "Lofi Campaign Planner - multi-agent campaign planning workflow"
requires-python = ">=3.13"
dependencies = [
    "langgraph>=0.2",
    "boto3>=1.34",
    "supabase>=2.6",
]

[tool.uv]
package = false
