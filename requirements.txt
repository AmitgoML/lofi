# ── Runtime ───────────────────────────────────────────────────────────────────

# API layer
fastapi>=0.115
uvicorn[standard]>=0.30

# Multi-agent workflow orchestration
langgraph>=0.2

# AWS (Bedrock LLM + Bedrock Titan image gen + S3 storage)
boto3>=1.34
botocore>=1.34

# Supabase (brand data + campaign metrics)
supabase>=2.6

# Data validation / serialization
pydantic>=2.7
pydantic-settings>=2.3

# ── Dev / Testing ─────────────────────────────────────────────────────────────

pytest>=8.0
httpx>=0.27          # FastAPI test client
pytest-asyncio>=0.23 # async test support
